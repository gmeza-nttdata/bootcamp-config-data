server:
  port: 8080
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: users-service
          uri: http://localhost:8081/users/
          predicates:
            - Path=/users/**
        - id: products-service
          uri: http://localhost:8082/accounts/
          predicates:
            - Path=/accounts/**
        - id: credits-service
          uri: http://localhost:8083/credits/
          predicates:
            - Path=/credits/**
